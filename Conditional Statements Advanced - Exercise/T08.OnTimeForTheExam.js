function exam(input) {

    let examHour = Number(input[0]);
    let examMin = Number(input[1]);

    let arriveHour = Number(input[2]);
    let arriveMin = Number(input[3]);

    let allMinExamStart = (examHour * 60) + examMin;
    let allMinExamArrive = (arriveHour * 60) + arriveMin;

  


    


}

exam(["9", "30", "9", "50"]);



